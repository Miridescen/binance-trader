# 24h 实盘子账号密钥模板。
# 用法（在服务器上，不要提交真实密钥）：
#   cp .env.sub24h.example .env.sub24h
#   nano .env.sub24h        # 填入子账号 API Key / Secret
#   chmod 600 .env.sub24h
#
# 子账号 API Key 权限务必：✅ 允许合约  ❌ 关闭提现  🔒 IP 白名单 = 43.106.131.65
# 这个 .example 文件不含密钥，可提交；真实 .env.sub24h 已被 .gitignore 忽略。

BINANCE_API_KEY=在这里粘贴子账号_API_KEY
BINANCE_API_SECRET=在这里粘贴子账号_API_SECRET
